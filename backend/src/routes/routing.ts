import { Router } from 'express';

const router = Router();

// Placeholder for routing rules management
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Routing routes - Coming soon',
    data: [],
  });
});

export default router; 