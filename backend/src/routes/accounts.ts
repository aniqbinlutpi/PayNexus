import { Router } from 'express';

const router = Router();

// Placeholder for account management routes
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Account routes - Coming soon',
    data: [],
  });
});

export default router; 